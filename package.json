{
  "name": "www.danmconrad.com",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.5",
    "prop-types": "^15.6.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "git checkout -b master && git add -f build && git commit -am \"chore: deploy\"",
    "deploy": "git filter-branch -f --prune-empty --subdirectory-filter build && git push -f origin master && git checkout develop"
  },
  "homepage": "http://www.danmconrad.com"
}
